import { StyleSheet} from 'react-native';

const HotelStyles = StyleSheet.create({
    card: {
        width: '90%',
        marginLeft: '5%',
        marginTop: 10,
        backgroundColor: '#000'
    }
});
export default HotelStyles;